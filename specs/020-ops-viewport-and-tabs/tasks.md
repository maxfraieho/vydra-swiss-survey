# Tasks: 020-ops-viewport-and-tabs

- [x] T-200: Create contract schema `agent_intent.schema.json` and `web/src/types/agent.ts`.
- [x] T-201: Copy UI tokens and primitives (`web/src/ui/tokens.ts`, `web/src/ui/primitives.tsx`) and create `bin/ui_verify.sh`.
- [x] T-202: Implement `web/src/ops/useViewportMode.ts`, `web/src/ops/useTutorRelay.ts`, and `web/src/ops/Viewport.tsx`.
- [x] T-203: Decompose `SurveyOps.tsx` into modular subcomponents (`AttentionCard`, `AgentIntent`, `StepTimeline`, `LaunchForm`, `RecentRuns`, `SurveyOps`).
- [x] T-204: Refactor Information Architecture & Navigation in `web/src/shell/Nav.tsx` and `web/src/main.tsx` (5 items, default `/ops`).
- [x] T-205: Clean up HEX colors, styles, font sizes, and long files across screens (`SettingsPage`, `RulesTable`, `Traces`, `HostGate`, `Compare`, `Conflicts`).
- [x] T-206: Run verification scripts (`bin/ui_verify.sh`, `bin/sdd_verify.sh`, tests, build) and collect evidence screenshots.
