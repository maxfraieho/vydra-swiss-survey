# Sign-off Record: S4 (Node version on dev-184)

## Питання S4 (з плану)
Версія Node на dev-184

## Дефолт з плану
Потрібно перевірити перед U2; фіксуємо в `web/.nvmrc` + `engines`

## Факт (вивід з dev-184)
Команда виконання:
```bash
sshpass -p '805235io.' ssh -o StrictHostKeyChecking=no vokov@192.168.3.184 "node --version; npm --version"
```

Вивід:
```
v23.11.1
11.3.0
```

- **Node.js**: `v23.11.1`
- **npm**: `11.3.0`

## Висновок
- Питання S4 **підтверджено та закрито**.
- Значення для майбутнього `web/.nvmrc`: `23.11.1` (без префікса `v`).
- **`web/.nvmrc` САМ ще не створюється**: Директорії `web/` у репозиторії наразі немає (вона з'явиться у Фазі U2). Передчасне створення могло б конфліктувати зі скаффолдингом Фази U2.

## Доповнення (2026-08-08): build-хост змінено з dev-184 на телефон

План (§4.2) допускає "dev-184 (або будь-якому не-телефонному хості)". Перед дispatch-ем U2 перевірено фактичний стан ресурсів на кандидатах:

| Хост | RAM available | Swap used | Node | npm |
|---|---|---|---|---|
| dev-184 | 443M | 2.1Gi/4.1Gi | v23.11.1 | 11.3.0 |
| Orange Pi (.161) | 715Mi | 49Mi/2.2Gi | v20.19.2 | 9.2.0 |
| Телефон (цей пристрій, OnePlus 13, 16GB RAM, Snapdragon) | 4.4Gi | 5.7Gi/9.5Gi (некритично, buff/cache reclaimable) | **v26.4.0** | **11.19.0** |

Q явно вказав: телефон технічно найпотужніший з усіх трьох (більше пам'яті й процесора), і немає причин виносити збірку на слабший хост, якщо версія Node дозволяє. Перевірено: `llama-server`/vision зараз НЕ завантажені — конфлікту з heavy_state_busy() немає.

**Рішення**: U2 build виконується локально на телефоні, не на dev-184/Orange Pi.
**Значення для `web/.nvmrc`**: `26.4.0` (не `23.11.1` — попередній запис вище лишається як історичний факт про dev-184, але вже не є джерелом істини для build-хоста).
